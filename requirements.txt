# DeadSat Resurrection — AI-2 dependencies
# Install: pip install -r requirements.txt

# Core
fastapi==0.111.0
uvicorn==0.30.1
httpx==0.27.0
pydantic==2.7.4

# LangGraph (Recovery Agent)
langgraph==1.2.4
langchain-core==1.4.2

# Orbital mechanics (Contact Calculator)
sgp4==2.23

# ML / Anomaly detection (AI-1 uses these, but emulator streams to them)
# numpy==1.26.4
# torch==2.3.1

# Post-quantum crypto (CY-1 uses this — listed here for Pi setup)
# liboqs-python==0.10.0

# Dev / testing
pytest==8.2.2